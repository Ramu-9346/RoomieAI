/**
 * Root-level re-export of the Phase 2 design system components.
 * Import components from here OR from '@components' path alias.
 *
 * Both resolve to the same files in src/components/.
 */
export * from '../src/components';
